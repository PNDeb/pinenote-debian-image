# calls debootstrap
01_rootminfs.yaml

# installs base programs like network-manager, sudo, parted ...
02_baseprograms.yaml

# # Install local provided deb packages (like the kernel)
# localdebs.yaml

# add kernel, kernel modules, firmware files
03_addkernel.yaml

# # setup hostname, first user, initrd (using dracut)
04_finalsetup.yaml

05_gnome.yaml
06_custom_debs.yaml

07_gnome_customize.yaml

08_greeter.yaml

09_xournalpp.yaml

10_pin_custom_debs.yaml

90_adduser.yaml

# # create a gpt disk image, with one partition
# creatediskimage.yaml

# # take from the disk image only our partition
# takeoutpartition.yaml
98_create_disk_image.yaml
99_extract_partition_image.yaml
