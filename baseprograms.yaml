{{- $architecture := or .architecture "arm64" -}}
{{- $prevtargz := .prevtargz }}
{{- $targz := .targz }}

architecture: {{ $architecture }}

actions:
  - action: unpack
    file: {{ $prevtargz }}
    
  - action: apt
    recommends: false
    description: Install base programs
    packages:
      - sudo
      - ca-certificates
      - man-db
      - console-setup
      - console-data
      - parted
      - file
      - usbutils
      - bash-completion
      - bzip2
      - xz-utils
      - vim
      - w3m
      - mc
      - bluetooth
      - iw
      - network-manager
      - wget
      - rfkill
      - wireless-regdb
      - wireless-tools
      - wpasupplicant
      - ssh
      - alsa-utils
      - busybox
      - initramfs-tools
      - u-boot-menu
      - zstd

  - action: pack
    file: {{ $targz }}
